<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only the window background lives here: everything the app draws is themed from Compose
      (ui/theme/Color.kt). This one has to exist as a resource because the window is painted
      before the first frame composes, and it must match `colorScheme.background`.
    -->
    <color name="window_background">#FAFAFD</color>
</resources>
